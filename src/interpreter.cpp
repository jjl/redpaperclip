#include "redpaperclip/core/parser.hpp"

namespace redpaperclip {
namespace core {
  using namespace std;
  struct Interpreter {
    Response interpret (Cmd input);
  };
}
}

#include "redpaperclip/core.hpp"

#include <locale>
#include <string>

namespace redpaperclip {
namespace core {
  Response Interpreter::interpret(Cmd input) {
    
  }
}
}
